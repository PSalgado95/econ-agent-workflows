# Rerun Status

Status: structure-only toy bundle.

This bundle was written by hand as an example. It does not correspond to a real script, dataset, or rerun log.

For real review bundles, this file should name:

- the entrypoint used to regenerate outputs;
- the run date;
- the software environment;
- the git commit or version;
- and whether outputs are rerun, inspected-only, inherited, or structure-only.
