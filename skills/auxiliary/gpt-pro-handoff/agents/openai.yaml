interface:
  display_name: "GPT Pro Handoff"
  short_description: "Explicit user-requested GPT Pro packages only"
  default_prompt: "I explicitly request $gpt-pro-handoff in this current user turn. Prepare the bounded GPT Pro handoff package I describe from this workspace."
