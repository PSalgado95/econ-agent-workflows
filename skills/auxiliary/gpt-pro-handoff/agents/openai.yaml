interface:
  display_name: "GPT Pro Handoff"
  short_description: "Prepare an explicitly requested GPT Pro package"
  default_prompt: "Use $gpt-pro-handoff to prepare the external GPT Pro package I explicitly request in this current user turn."
policy:
  allow_implicit_invocation: false
